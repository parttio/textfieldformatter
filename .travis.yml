language: java
jdk:
  - openjdk11
sudo: true
dist: xenial

addons:
  apt:
    packages:
      - google-chrome-stable
env:
  - NODE_VERSION="12.14.0"
before_install:
  - nvm install $NODE_VERSION
install: true
script: sh ./.travis.script.sh
